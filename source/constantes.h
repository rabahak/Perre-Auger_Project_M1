#if !defined(MYLIB_CONSTANTS_H)
#define MYLIB_CONSTANTS_H 1
#include <cmath>
const double d=1500; //la cote du triangle(la maille) - distance entre deux cuves
const double r=(sqrt(3)/3)*d; // le circumradius (rayon du cercle qui engendre le triangle
const double h=(sqrt(3)/2)*d; // la hauteur du triangle equilateral(la maille)
#endif
